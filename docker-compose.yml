
version: '3'

services:

  api:
    build: ./backend/api
    command: python app.py
    ports:
      - "8000:8000"

  ml:
    build: ./ml-engine/service
    command: python ml_service.py
    ports:
      - "8001:8001"
